function Footer(props, category= "fruit", items=["Nothing to show"]) {
    const itemsList = props.items
    // const filteredItems = itemsList.filter(item => item.age > 23);

    return (
        <>
            
        </>
        );
}

export default Footer;